---
import HomePage from '../components/HomePage.astro';

interface Frontmatter {
  title: string;
  description: string;
  date: string;
  languageKey: string;
  languageLink: string;
  socialImage: string;
  slug: string;
}

const posts = await Astro.glob<Frontmatter>('./blog/en/**/*.md');
---

<HomePage posts={posts} lang="en" />

